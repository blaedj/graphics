#include <cstdlib>
#include "camera.h"

using namespace raytracer;

int main(int argc, char *argv[])
{


  //  if (cam.CameraIsValid()) {
  exit(EXIT_SUCCESS);
  //  } else {
  exit(EXIT_FAILURE);
  //  }


}
