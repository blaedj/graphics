#include "cube.h"

using namespace sivelab;


void Cube::render(){
  // the following go before the cube is rendered.
  // glClear();
  // glMatrixMode(GL_MODELVIEW);
  // glLoadIdentity();
  // gluLookAt( 0, 0, 5, 0, 0, 0, 0, 1, 0 );
  // use glBindBuffer(GL_ARRAY_BUFFER, 0); to do the actual drawing, after any transforms

}
